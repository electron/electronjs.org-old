language: node_js

node_js:
  - "12"

notifications:
  email: false

jobs:
  include:
    - stage: "Tests"
      name: "Unit tests"
      script: npm test
    - script: npm run integration
      name: "Integration Tests"
